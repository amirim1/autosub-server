fastapi
uvicorn
httpx
python-multipart
jinja2
aiosqlite
pyyaml
# anyio needs this backport on Python 3.10. Keep it explicit so hash locks compiled
# on newer Python versions remain installable across the supported CI matrix.
exceptiongroup==1.3.1
